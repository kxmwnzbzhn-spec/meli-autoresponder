name: daily-pro
# 1 workflow, 4 jobs (3 build en paralelo + 1 combine).
# Disparado por Supabase pg_cron a las 13:05 UTC (7:05 AM CDMX).
on:
  workflow_dispatch:
    inputs:
      force_regen:
        description: 'Regenerar aunque ya exista PDF de hoy'
        required: false
        type: string
        default: ''

jobs:
  sunday-guard:
    runs-on: ubuntu-latest
    outputs:
      run: ${{ steps.check.outputs.run }}
    steps:
      - id: check
        name: Permitir solo lunes a sábado CDMX
        run: |
          if [ "$(TZ=America/Mexico_City date +%u)" = "7" ]; then
            echo "run=false" >> "$GITHUB_OUTPUT"
            echo "Domingo: daily-pro omitido"
          else
            echo "run=true" >> "$GITHUB_OUTPUT"
          fi

  build-claribel:
    needs: sunday-guard
    if: needs.sunday-guard.outputs.run == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: '3.11' }
      - run: sudo apt-get update && sudo apt-get install -y poppler-utils
      - run: pip install requests pypdf reportlab pdf2image Pillow -q
      - env:
          ACCOUNT: Claribel
          MELI_APP_ID:                 ${{ secrets.MELI_APP_ID }}
          MELI_APP_SECRET:             ${{ secrets.MELI_APP_SECRET }}
          MELI_REFRESH_TOKEN_CLARIBEL: ${{ secrets.MELI_REFRESH_TOKEN_CLARIBEL }}
          SUPABASE_URL:             ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY:     ${{ secrets.SUPABASE_SERVICE_KEY }}
        run: python scripts/labels_one.py
      - uses: actions/upload-artifact@v4
        with:
          name: labels-Claribel
          path: ETIQUETAS_CLARIBEL.pdf
          if-no-files-found: error
          retention-days: 7

  build-asva:
    needs: sunday-guard
    if: needs.sunday-guard.outputs.run == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: '3.11' }
      - run: sudo apt-get update && sudo apt-get install -y poppler-utils
      - run: pip install requests pypdf reportlab pdf2image Pillow -q
      - env:
          ACCOUNT: Asva
          MELI_APP_ID:             ${{ secrets.MELI_APP_ID }}
          MELI_APP_SECRET:         ${{ secrets.MELI_APP_SECRET }}
          MELI_REFRESH_TOKEN_ASVA: ${{ secrets.MELI_REFRESH_TOKEN_ASVA }}
          SUPABASE_URL:             ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY:     ${{ secrets.SUPABASE_SERVICE_KEY }}
        run: python scripts/labels_one.py
      - uses: actions/upload-artifact@v4
        with:
          name: labels-Asva
          path: ETIQUETAS_ASVA.pdf
          if-no-files-found: error
          retention-days: 7

  build-adrian:
    needs: sunday-guard
    if: needs.sunday-guard.outputs.run == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: '3.11' }
      - run: sudo apt-get update && sudo apt-get install -y poppler-utils
      - run: pip install requests pypdf reportlab pdf2image Pillow -q
      - env:
          ACCOUNT: Ah
          MELI_APP_ID:           ${{ secrets.MELI_APP_ID }}
          MELI_APP_SECRET:       ${{ secrets.MELI_APP_SECRET }}
          MELI_REFRESH_TOKEN_AH: ${{ secrets.MELI_REFRESH_TOKEN_AH }}
          SUPABASE_URL:             ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY:     ${{ secrets.SUPABASE_SERVICE_KEY }}
        run: python scripts/labels_one.py
      - uses: actions/upload-artifact@v4
        with:
          name: labels-Ah
          path: ETIQUETAS_AH.pdf
          if-no-files-found: error
          retention-days: 7

  build-mc:
    needs: sunday-guard
    if: needs.sunday-guard.outputs.run == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: '3.11' }
      - run: |
          sudo apt-get update || true
          sudo apt-get install -y poppler-utils || (sudo apt-get update && sudo apt-get install -y poppler-utils)
      - run: pip install requests pypdf reportlab pdf2image Pillow -q
      - env:
          ACCOUNT: Mc
          MELI_APP_ID:             ${{ secrets.MELI_APP_ID }}
          MELI_APP_SECRET:         ${{ secrets.MELI_APP_SECRET }}
          MELI_REFRESH_TOKEN_MC:   ${{ secrets.MELI_REFRESH_TOKEN_MC }}
          SUPABASE_URL:            ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY:    ${{ secrets.SUPABASE_SERVICE_KEY }}
        run: python scripts/labels_one.py
      - uses: actions/upload-artifact@v4
        with:
          name: labels-Mc
          path: ETIQUETAS_MC.pdf
          if-no-files-found: error
          retention-days: 7

  build-lupita:
    needs: sunday-guard
    if: needs.sunday-guard.outputs.run == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: '3.11' }
      - run: |
          sudo apt-get update || true
          sudo apt-get install -y poppler-utils || (sudo apt-get update && sudo apt-get install -y poppler-utils)
      - run: pip install requests pypdf reportlab pdf2image Pillow -q
      - env:
          ACCOUNT: Lupita
          MELI_APP_ID:               ${{ secrets.MELI_APP_ID }}
          MELI_APP_SECRET:           ${{ secrets.MELI_APP_SECRET }}
          MELI_REFRESH_TOKEN_LUPITA: ${{ secrets.MELI_REFRESH_TOKEN_LUPITA }}
          SUPABASE_URL:              ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY:      ${{ secrets.SUPABASE_SERVICE_KEY }}
        run: python scripts/labels_one.py
      - uses: actions/upload-artifact@v4
        with:
          name: labels-Lupita
          path: ETIQUETAS_LUPITA.pdf
          if-no-files-found: error
          retention-days: 7

  build-karime:
    needs: sunday-guard
    if: needs.sunday-guard.outputs.run == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: '3.11' }
      - run: |
          sudo apt-get update || true
          sudo apt-get install -y poppler-utils || (sudo apt-get update && sudo apt-get install -y poppler-utils)
      - run: pip install requests pypdf reportlab pdf2image Pillow -q
      - env:
          ACCOUNT: Karime
          MELI_APP_ID:               ${{ secrets.MELI_APP_ID }}
          MELI_APP_SECRET:           ${{ secrets.MELI_APP_SECRET }}
          MELI_REFRESH_TOKEN_KARIME: ${{ secrets.MELI_REFRESH_TOKEN_KARIME }}
          SUPABASE_URL:              ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY:      ${{ secrets.SUPABASE_SERVICE_KEY }}
        run: python scripts/labels_one.py
      - uses: actions/upload-artifact@v4
        with:
          name: labels-Karime
          path: ETIQUETAS_KARIME.pdf
          if-no-files-found: error
          retention-days: 7

  build-ligia:
    needs: sunday-guard
    if: needs.sunday-guard.outputs.run == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: '3.11' }
      - run: |
          sudo apt-get update || true
          sudo apt-get install -y poppler-utils || (sudo apt-get update && sudo apt-get install -y poppler-utils)
      - run: pip install requests pypdf reportlab pdf2image Pillow -q
      - env:
          ACCOUNT: Ligia
          MELI_APP_ID:              ${{ secrets.MELI_APP_ID }}
          MELI_APP_SECRET:          ${{ secrets.MELI_APP_SECRET }}
          MELI_REFRESH_TOKEN_LIGIA: ${{ secrets.MELI_REFRESH_TOKEN_LIGIA }}
          SUPABASE_URL:             ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY:     ${{ secrets.SUPABASE_SERVICE_KEY }}
        run: python scripts/labels_one.py
      - uses: actions/upload-artifact@v4
        with:
          name: labels-Ligia
          path: ETIQUETAS_LIGIA.pdf
          if-no-files-found: error
          retention-days: 7

  combine-and-upload:
    needs: [build-claribel, build-asva, build-adrian, build-mc, build-lupita, build-karime, build-ligia]
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: { python-version: '3.11' }
      - run: pip install requests pypdf google-api-python-client google-auth google-auth-oauthlib -q
      - uses: actions/download-artifact@v4
        with:
          path: ./pdfs
      - run: ls -R ./pdfs
      - env:
          FORCE_REGEN:                ${{ inputs.force_regen }}
          GOOGLE_OAUTH_CLIENT_ID:     ${{ secrets.GOOGLE_OAUTH_CLIENT_ID }}
          GOOGLE_OAUTH_CLIENT_SECRET: ${{ secrets.GOOGLE_OAUTH_CLIENT_SECRET }}
          GOOGLE_OAUTH_REFRESH_TOKEN: ${{ secrets.GOOGLE_OAUTH_REFRESH_TOKEN }}
          TELEGRAM_BOT_TOKEN:         ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID:           ${{ secrets.TELEGRAM_CHAT_ID }}
          DRIVE_FOLDER_ID:            '1aIDN3iq6zwCacL57iamptvQCPoSDyRbL'
          SUPABASE_URL:               ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY:       ${{ secrets.SUPABASE_SERVICE_KEY }}
          GITHUB_RUN_ID:              ${{ github.run_id }}
        run: python scripts/daily_combine.py
