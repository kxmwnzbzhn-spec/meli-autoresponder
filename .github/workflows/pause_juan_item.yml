name: pause-juan-item
on: { workflow_dispatch: {} }
jobs:
  go:
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - run: pip install requests -q
      - env:
          MELI_APP_ID: ${{ secrets.MELI_APP_ID }}
          MELI_APP_SECRET: ${{ secrets.MELI_APP_SECRET }}
          MELI_REFRESH_TOKEN: ${{ secrets.MELI_REFRESH_TOKEN }}
        run: |
          python3 - <<'PY'
          import os, requests
          API="https://api.mercadolibre.com"
          tok=requests.post(f"{API}/oauth/token",data={"grant_type":"refresh_token",
              "client_id":os.environ["MELI_APP_ID"],
              "client_secret":os.environ["MELI_APP_SECRET"],
              "refresh_token":os.environ["MELI_REFRESH_TOKEN"]},timeout=20).json()["access_token"]
          h={"Authorization":f"Bearer {tok}"}
          hj={**h,"Content-Type":"application/json"}
          IID="MLM5244765752"
          it=requests.get(f"{API}/items/{IID}",headers=h).json()
          print(f"Antes: status={it.get('status')} has_bids={it.get('sub_status') or 'n/a'} title='{it.get('title','')[:55]}'")
          r=requests.put(f"{API}/items/{IID}",headers=hj,json={"status":"paused"},timeout=15)
          print(f"PUT pause: {r.status_code} {r.text[:300]}")
          PY
