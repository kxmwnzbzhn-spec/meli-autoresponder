name: pause-2-final
on: { workflow_dispatch: {} }
jobs:
  go:
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - run: pip install requests -q
      - env:
          MELI_APP_ID: ${{ secrets.MELI_APP_ID }}
          MELI_APP_SECRET: ${{ secrets.MELI_APP_SECRET }}
          MELI_REFRESH_TOKEN_WILBERT: ${{ secrets.MELI_REFRESH_TOKEN_WILBERT }}
        run: |
          python3 - <<'PY'
          import os, requests
          API="https://api.mercadolibre.com"
          tok=requests.post(f"{API}/oauth/token",data={"grant_type":"refresh_token",
              "client_id":os.environ["MELI_APP_ID"],
              "client_secret":os.environ["MELI_APP_SECRET"],
              "refresh_token":os.environ["MELI_REFRESH_TOKEN_WILBERT"]},timeout=20).json()["access_token"]
          hj={"Authorization":f"Bearer {tok}","Content-Type":"application/json"}
          for iid in ("MLM5265893628","MLM5265867982"):
              it=requests.get(f"{API}/items/{iid}",headers={"Authorization":f"Bearer {tok}"}).json()
              print(f"{iid} antes: status={it.get('status')} qty={it.get('available_quantity')}")
              r=requests.put(f"{API}/items/{iid}",headers=hj,json={"status":"paused"},timeout=15)
              print(f"  pause: {r.status_code}")
          PY
