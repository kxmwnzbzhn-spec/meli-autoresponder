name: asva-flip7-v3
on:
  workflow_dispatch:
jobs:
  run:
    runs-on: ubuntu-latest
    permissions: {contents: write}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.11'}
      - run: pip install requests -q
      - env:
          MELI_APP_ID: ${{ secrets.MELI_APP_ID }}
          MELI_APP_SECRET: ${{ secrets.MELI_APP_SECRET }}
          MELI_REFRESH_TOKEN_ASVA: ${{ secrets.MELI_REFRESH_TOKEN_ASVA }}
        run: python scripts/asva_flip7_v3.py
      - run: |
          git config user.name bot
          git config user.email b@b.com
          git add stock_config_asva.json 2>/dev/null
          git diff --cached --quiet || git commit -m "ASVA Bocina IP67 publicada"
          git push || true
