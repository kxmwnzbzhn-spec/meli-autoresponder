name: stock-aqua-men
on: { workflow_dispatch: {} }
jobs:
  go:
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - run: pip install requests -q
      - env:
          MELI_APP_ID: ${{ secrets.MELI_APP_ID }}
          MELI_APP_SECRET: ${{ secrets.MELI_APP_SECRET }}
          MELI_REFRESH_TOKEN_WILBERT: ${{ secrets.MELI_REFRESH_TOKEN_WILBERT }}
        run: |
          python3 - <<'PY'
          import os, requests
          API="https://api.mercadolibre.com"
          tok=requests.post(f"{API}/oauth/token",data={"grant_type":"refresh_token",
              "client_id":os.environ["MELI_APP_ID"],
              "client_secret":os.environ["MELI_APP_SECRET"],
              "refresh_token":os.environ["MELI_REFRESH_TOKEN_WILBERT"]},timeout=20).json()["access_token"]
          h={"Authorization":f"Bearer {tok}"}
          hj={**h,"Content-Type":"application/json"}
          IID="MLM5265906396"
          it=requests.get(f"{API}/items/{IID}",headers=h).json()
          print(f"Antes: status={it.get('status')} qty={it.get('available_quantity')} title='{it.get('title','')[:60]}'")
          r=requests.put(f"{API}/items/{IID}",headers=hj,json={"available_quantity":1,"status":"active"},timeout=15)
          print(f"PUT qty=1 active: {r.status_code} {r.text[:200]}")
          it2=requests.get(f"{API}/items/{IID}",headers=h).json()
          print(f"Despues: status={it2.get('status')} qty={it2.get('available_quantity')}")
          PY
